<%= form_with(model: [job, job_application], local: true) do |form| %>
  <% if job_application.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(job_application.errors.count, "error") %> prohibited this job_application from being saved:</h2>

      <ul>
        <% job_application.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

    <div class="form-group width-50 mb-4">
      <%= form.label :full_name %>
      <%= form.text_field :full_name, placeholder: 'John Johnson', class: 'form-control' %>
    </div>

    <div class="form-group width-50 mb-4">
      <%= form.label :birthday %>
      <%= form.date_field :date_of_birth, class: 'form-control' %>
    </div>

    <div class="form-group width-50 mb-4">
      <%= form.label :email %>
      <%= form.email_field :email, type: 'email',placeholder: 'name@example.com', class: 'form-control' %>
    </div>

    <div class="form-group width-50 mb-4">
      <%= form.label :phone_number %>
      <%= form.text_field :phone_number, placeholder: '+123123123', class: 'form-control' %>
    </div>

    <div class="form-group width-50 mb-4">
      <%= form.label :address %>
      <%= form.text_field :address, placeholder: 'Address 123', class: 'form-control' %>
    </div>

    <div class="form-group width-50 mb-4">
      <p class="mb-1"><%= form.label :education %></p>
      <%= form.select :education, [['Visa strucna sprema', 'vss'], ['nk'],
                                                    ['všs'], ['Sredna strucna sprema', 'sss']], class: 'form-control' %>
    </div>

  <div class="form-group width-25 mb-4">
    <%= form.submit 'Submit proposal', class: 'form-control' %>
  </div>
<% end %>
